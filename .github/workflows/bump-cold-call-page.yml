name: Bump cold call page asset
on:
  push:
    branches: [main]
    paths:
      - 'cold-call-voice-v5.js'
      - 'site-repair.js'
      - '.github/workflows/bump-cold-call-page.yml'
permissions:
  contents: write
jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Update cold call runtime tags
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          s=s.replace('/cold-call-voice-v5.js?v=20260905-voice-v5','/cold-call-voice-v5.js?v=20260906-page-2')
          import re
          s=re.sub(r'\s*<script src="/cold-call-page-mode\.js\?v=[^"]+"></script>','',s)
          s=re.sub(r'\s*<script src="/cold-call-page-v4\.js\?v=[^"]+"></script>','',s)
          p.write_text(s,encoding='utf-8')
          PY
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet && exit 0
          git commit -m 'fix: use normal cold call training page'
          git push
